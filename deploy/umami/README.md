# Umami Integration

APwhy pulls traffic analytics from Umami using:

- `UMAMI_BASE_URL`
- `UMAMI_API_KEY`
- `UMAMI_WEBSITE_ID`

Set these in `.env` for API-level traffic charts. Client-side APwhy events are also tracked in APwhy's own metrics store.
