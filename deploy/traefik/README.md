# Traefik Notes

Traefik is configured via Docker labels in `deploy/docker-compose.yml`.

- APwhy route: `Host(${APWHY_DOMAIN})`
- Umami route: `Host(${UMAMI_DOMAIN})`
- Dashboard: `http://localhost:8080`
